# Streamlit secrets — copy to secrets.toml for local dev, or paste into
# Streamlit Cloud dashboard under Settings → Secrets.
#
# Local dev (Docker):
#   API_URL = "http://localhost:8000"
#
# Streamlit Cloud → Cloud Run:
#   API_URL = "https://your-cloud-run-service-url"

API_URL = "http://localhost:8000"
